module.exports = {
    //endpoint: 'https://dev.azure.com/{organizationName}/', // You can specify the URI here, or read it from the Azure DevOps pipeline (see renovate-pipeline.yaml).
    platform: 'azure',
    logLevel: 'debug',
};